# PASS242

Command Palette + Mission Control spacing polish and `1.9.0` version lane.

Run:

```powershell
Set-Location C:\dev\browser\app
node scripts\apply-pass242-command-palette-mission-spacing-version.mjs
npm run verify:pass-242-command-palette-mission-spacing-version
npm run typecheck
npm run build
```
