TAHAI Web Services Browser 1.8.30 RC / Mission Control Build

This release gives the main download page fanfare to the newest Windows RC installers while retaining the older 1.8.6 quad-view test build and the 1.8.0 public-preview installer links.

Installers are intentionally unsigned at this stage. Verify SHA-256 before running.

Because Cloudflare Pages has a 25 MiB single-asset limit, the installer binaries are not embedded in this deploy ZIP. Upload the EXE/MSI to an external file host, R2, release attachment, or a later dedicated download domain, then keep the canonical paths documented on the website.
