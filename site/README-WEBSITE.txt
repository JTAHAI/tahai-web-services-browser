TAHAI BROWSER — GITHUB PAGES SITE PACKAGE
Website release: 20260913-operational-everyone
Positioning: THE Operational Browser. For Everyone.

Extract at repository root so the files are under site/.
This is the website only. No browser source, release package, or workflow changes
are included. The existing Pages workflow can publish the site/ directory.
The matching Cloudflare ZIP has index.html at its archive root.

Subpage assets use relative paths; the custom 404 uses explicit project paths
because 404 documents can be served from arbitrary requested URLs.
Canonical and social metadata point to the primary browser.tahai.net website.
The new social asset exists in both packages; deploy both for matching branding.

Review website-only changes without resetting or overwriting native browser work.
Codex's scope is the native browser, not these websites. No runtime MSIX is here.
No upload or commit is implied by possession of this local package.
