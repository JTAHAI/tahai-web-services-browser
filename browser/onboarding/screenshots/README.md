# In-app KB screenshot mirror

This folder is the in-app mirror for approved KB screenshots.

Populate it by running:

```powershell
npm run kb:screenshots:ingest -- --apply
```

Do not place runtime browser profiles, generated installers, customer screenshots with secrets, or local data here.
